import { GetUsersAccessPipe } from './get-users-access.pipe';

describe('GetUsersAccessPipe', () => {
  it('create an instance', () => {
    const pipe = new GetUsersAccessPipe();
    expect(pipe).toBeTruthy();
  });
});
