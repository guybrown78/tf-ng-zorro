import { GetSecondsToHMSPipe } from './get-seconds-to-h-m-s.pipe';

describe('GetSecondsToHMSPipe', () => {
  it('create an instance', () => {
    const pipe = new GetSecondsToHMSPipe();
    expect(pipe).toBeTruthy();
  });
});
