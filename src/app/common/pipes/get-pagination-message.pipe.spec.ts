import { GetPaginationMessagePipe } from './get-pagination-message.pipe';

describe('GetPaginationMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new GetPaginationMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
