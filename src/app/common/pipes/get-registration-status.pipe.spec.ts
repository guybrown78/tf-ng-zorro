import { GetRegistrationStatusPipe } from './get-registration-status.pipe';

describe('GetRegistrationStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new GetRegistrationStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
